class HomeController < ApplicationController
  def show
    # @game = Game.create(session_id: session.id.to_s).first_or_initialize(counter: 17800)
    # @game = Game.create
    # redirect_to game_url(@game)
  end
end
